<section data-section-id="{{ section.id }}">
  {%- if product -%}
      {% render 'breadcrumbs' %}
  {%- endif -%}
</section>

{% schema %}
{
  "name": "t:general.product",
  "settings": [
  ],
  "disabled_on": {
    "groups": ["header", "footer"]
  }
}
{% endschema %}
